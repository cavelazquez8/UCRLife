export interface Offer {
    _id: string,
    title: string,
    username?: string,
    description?: string,
    imgURL?: string,
    price: number,
    categogry?: string,
    createdAt: string,
    updatedAt: string,
}